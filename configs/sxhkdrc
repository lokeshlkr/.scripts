# re-mapped super key by itself
#F34
#    {s gui}

super + {Return,q,w,g,e,l,t,s}
    {alacritty,rofi -show drun,firefox,gpick,thunar,xflock4,alacritty,s gui}

super + {shift + r,shift + c,shift + s}
    {s refresh ,gpick,xfce4-screenshooter -rc}

# re-mapped capslock key
#F35
#    {s gui}
