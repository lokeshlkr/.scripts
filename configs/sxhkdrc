# re-mapped super key by itself
F34
    {s gui}

super + {Return,r,w,g,e,l,t}
    {alacritty,rofi -show drun,firefox,gpick,thunar,xflock4,alacritty}

super + {shift + r,1}
    {s restart sxhkd,s paste}

# re-mapped capslock key
F35
    {s gui}

